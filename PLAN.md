# Casewright — Plan & Status Ledger

Source spec: `casewright-implementation-plan.md`. This file tracks what was intended vs. what
has passed review, one checklist item per task.

## Phase 0 — Foundation

Exit criterion (plan §14): two firms cannot see each other's data; deploy from clean machine in <15 min.

- [x] T0.1: Repo scaffold, Docker Compose (db/minio/backend/frontend/nginx), nginx reverse proxy, `.env.example` — acceptance: `docker compose up -d --build` brings up all 5 services healthy · reviewed 2026-07-21 @ PENDING_SHA
- [x] T0.2: SQLAlchemy async models for all 14 tables in plan §4 (firms, users, cases, documents, extracted_facts, criterion_assessments, strategy_memos, drafts, draft_sections, citations, rfe_notices, rfe_objections, agent_runs, audit_log, billing_events, knowledge_chunks) with UUID PKs, timestamps, firm_id tenancy — acceptance: `Base.metadata` reflects all tables, `mypy app` passes · reviewed 2026-07-21 @ PENDING_SHA
- [x] T0.3: Alembic migration (autogenerated + hand-fixed for pgvector extension/import), audit_log append-only enforced via a `BEFORE UPDATE OR DELETE` trigger (stronger than a GRANT — binds the table owner too) — acceptance: `alembic upgrade head` runs clean on a fresh db; direct `UPDATE`/`DELETE` on `audit_log` raises · reviewed 2026-07-21 @ PENDING_SHA
- [x] T0.4: JWT auth (bcrypt password hashing, short-lived tokens) + RBAC role check dependency — acceptance: `POST /api/auth/login`, `GET /api/auth/me` work; wrong-password and unknown-user return identical 401 · reviewed 2026-07-21 @ PENDING_SHA
- [x] T0.5: Tenancy scoping — single `get_case_scoped` dependency, no other code path queries `cases` unscoped — acceptance: cross-firm `GET /api/cases/{id}` returns 404, not 403 (no existence leak); firm B's `GET /api/cases` list never contains firm A rows · reviewed 2026-07-21 @ PENDING_SHA
- [x] T0.6: Audit log service (`services/audit.py`), wired into case creation — acceptance: `case.created` audit row is written atomically with the case insert (same transaction) · reviewed 2026-07-21 @ PENDING_SHA
- [x] T0.7: `scripts/create_firm.py` (firm + admin onboarding), `scripts/seed_knowledge.py` (Phase 1 placeholder) — acceptance: runs inside the `backend` container per the deploy sequence in §11 · reviewed 2026-07-21 @ PENDING_SHA
- [x] T0.8: Integration tests (real Postgres, not sqlite) — cross-firm isolation test (non-negotiable per §13) + auth tests — acceptance: `pytest` passes, 5/5 · reviewed 2026-07-21 @ PENDING_SHA
- [x] T0.9: CI (`.github/workflows/ci.yml`) — lint (ruff) → typecheck (mypy) → migrate → test → build (backend + frontend images) — acceptance: workflow file present and mirrors the exact commands verified locally · reviewed 2026-07-21 @ PENDING_SHA
- [x] T0.10: Minimal frontend shell (React + Vite + Tailwind, design tokens from §9, login + dashboard) — acceptance: `npm run build` succeeds, served by nginx, login flow works against the live API · reviewed 2026-07-21 @ PENDING_SHA

**Phase 0 verified end-to-end 2026-07-21**: `docker compose up -d --build` (~4m40s including image pulls, well under the 15-min budget) → `scripts/create_firm.py` for two firms → login as each → firm A creates a case → firm B's direct-id read 404s, firm B's list is empty, firm A's own read succeeds, unauthenticated read is 401. See `PROJECT_LOG.md` for detail.

## Phase 1 — RFE engine (not started)

Doc upload + OCR, knowledge seed, RFE graph (parse → plan → draft → verify), runner + gates,
minimal review UI. Exit criterion: real RFE notice in → verified rebuttal draft out, gated, audited.

- [ ] T1.1: S3/MinIO document upload endpoint + native PDF text extraction + vision OCR fallback (<40 chars/page threshold)
- [ ] T1.2: `knowledge_chunks` seeding (18 criterion standards, core authorities, starter patterns) + embeddings (Voyage, hash fallback)
- [ ] T1.3: LangGraph RFE graph: parse → plan → draft → verification → interrupt gate → finalize, `AsyncPostgresSaver` checkpointing
- [ ] T1.4: Runner module (`start_run`/`resume_run`), `agent_runs` lifecycle
- [ ] T1.5: Verification layer (citation integrity, fact consistency, confidence surfacing)
- [ ] T1.6: `POST /cases/{id}/runs/rfe`, `GET /cases/{id}/runs`, `POST /runs/{run_id}/gate`, `GET /cases/{id}/rfe`
- [ ] T1.7: Minimal RFE review UI

## Phase 2 — Petition engine (not started)
## Phase 3 — Product UI (not started)
## Phase 4 — Pilot hardening (not started)

See `casewright-implementation-plan.md` §14 for full phase contents.
